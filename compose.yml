version: '1.0.0'
services:
  api:
    image: rooms-api-image
    container_name: rooms-api-container
    depends_on: []
    environment:
      DB_HOST: host.containers.internal
      DB_USER: code
      DB_PASSWORD: dev</>
      DB_NAME: cis-rooms
    ports:
      - "6000:6000"
